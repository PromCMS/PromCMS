import { Context, ContextProvider, useData } from './context'
import { Content } from './Content'

const UserUnderpages = {
  Context,
  ContextProvider,
  useData,
  Content,
}

export default UserUnderpages
