{
  project: {
    name: '<%= project.name %>',
    slug: '<%= project.slug %>',
    url: 'http://localhost',
  },
  database: {
    connections: [
      {
        name: 'default-connection',
        uri: 'pdo-sqlite:///.database/application.sqlite',
      }
    ]
  }
}