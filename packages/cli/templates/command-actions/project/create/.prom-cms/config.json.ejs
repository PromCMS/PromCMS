{
  project: {
    name: '<%= project.name %>',
    slug: '<%= project.slug %>',
    url: 'http://localhost',
  },
  database: {
    connections: [
      {
        name: 'default-connection',
        adapter: 'sqlite',
        dsn: 'sqlite:<%= project.root %>/.database/application.sq3',
        user: 'root',
        password: null
      }
    ]
  }
}