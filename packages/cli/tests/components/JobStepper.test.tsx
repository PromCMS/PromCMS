import { describe, it } from 'vitest';
import { render } from 'ink-testing-library';

describe('ink components', () => {
  describe('JobStepper', () => {
    it('should render each step', () => {
      //const {lastFrame, rerender} = render(<Counter count={0}/>);
    });
  });
});
