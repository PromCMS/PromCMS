{
  "name": "prom-cms",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "regenerate:dev": "yarn workspace @prom-cms/cli run regenerate:dev",
    "generate:dev": "yarn workspace @prom-cms/cli run generate:dev",
    "build:packages": "turbo run build --filter=./packages/*",
    "build:admin": "turbo run build --filter=@prom-cms/admin",
    "dev:admin": "yarn workspace @prom-cms/admin run dev",
    "dev:api": "yarn workspace @prom-cms/dev-api run dev",
    "dev": "npx concurrently \"npm run dev:api\" \"npm run dev:admin\"",
    "release": "yarn build:library && changeset publish",
    "clean": "del **/dist/** && del **/.next/**",
    "prettier": "prettier --config ./packages/config/prettier-config.js --write \"**/*.{ts,tsx}\"",
    "version-packages": "changeset version && yarn prettier"
  },
  "license": "MIT",
  "workspaces": [
    "./packages/*",
    "./apps/*"
  ],
  "publishConfig": {
    "access": "restricted",
    "registry": "https://registry.npmjs.org/"
  },
  "contributors": [
    "Ondřej Langr <hi@ondrejlangr.cz>"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.22.0",
    "@prettier/plugin-php": "0.17.6",
    "@swc/core": "^1.3.21",
    "caniuse-lite": "^1.0.30001387",
    "concurrently": "^7.2.2",
    "del-cli": "4.0.1",
    "prettier": "2.7.1",
    "tsup": "^6.5.0",
    "rimraf": "3.0.2",
    "turbo": "^1.6.3"
  },
  "overrides": {
    "react": "^18.2.0",
    "@types/react": "^18.0.24"
  }
}
