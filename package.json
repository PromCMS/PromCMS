{
  "name": "prom-cms",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "dev": "turbo run dev --scope @prom-cms/mock-api --scope @prom-cms/admin --parallel",
    "dev:api": "turbo run dev --scope @prom-cms/mock-api --parallel",
    "sync-database": "php packages/core/app/scripts/sync-database.php",
    "generate:dev": "yarn workspace @prom-cms/cli run generate:dev",
    "generate:cms": "yarn workspace @prom-cms/cli run generate:cms"
  },
  "license": "ISC",
  "workspaces": [
    "./packages/*"
  ],
  "devDependencies": {
    "@prettier/plugin-php": "0.17.6",
    "@swc/core": "1.2.120",
    "@types/node": "16.11.10",
    "rimraf": "3.0.2",
    "turbo": "^1.1.10",
    "typescript": "4.5.2"
  },
  "dependencies": {
    "@changesets/cli": "^2.22.0"
  }
}
